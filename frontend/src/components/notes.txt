BlueArc LTD delivers advanced industrial solutions specializing in pumps, water & waste systems, fabrication, construction, and repairs.
                    We proudly support industries across Kenya, offering engineering services that meet the highest standards of performance, safety, and durability. Every project we undertake is guided by our commitment to quality workmanship and long-term client satisfaction.
                    Our commitment to quality, safety, and client satisfaction drives every project we undertake.
                
    const capabilities = [
        {
            title: 'Fabrication Solutions',
            desc: 'Precision metal fabrication for industrial, commercial, and construction applications.',
            details: 'We provide custom steelworks, welding, structural fabrication, and tailored components built to withstand demanding environments.',
            icon: <Hammer size={32} strokeWidth={1.5} />,
            color: '#f59e0b' // Amber/Orange
        },
        {
            title: 'Construction Services',
            desc: 'Reliable construction support services for industrial and infrastructure projects.',
            details: 'Our team executes structural works, installations, on-site assembly, and project support with a focus on durability and safety.',
            icon: <Building size={32} strokeWidth={1.5} />,
            color: '#3b82f6' // Blue
        },
        {
            title: 'Repair & Maintenance',
            desc: 'Professional repair, servicing, and maintenance for industrial systems and equipment.',
            details: 'We troubleshoot, repair, and restore mechanical systems to optimal performance, minimizing downtime and extending equipment lifespan.',
            icon: <Wrench size={32} strokeWidth={1.5} />,
            color: '#ef4444' // Red
        },
        {
            title: 'Pump Systems',
            desc: 'Supply, installation, and servicing of industrial and commercial pumping systems.',
            details: 'We handle centrifugal, submersible, and specialty pumps with complete installation, repairs, and performance optimization.',
            icon: <Droplets size={32} strokeWidth={1.5} />,
            color: '#0ea5e9' // Light Blue
        },
        {
            title: 'Water Systems',
            desc: 'Solutions for water supply, treatment, and distribution systems.',
            details: 'We support borehole systems, filtration, storage, and piping installations to ensure reliable clean water flow.',
            icon: <Waves size={32} strokeWidth={1.5} />,
            color: '#10b981' // Green
        },
        {
            title: 'Waste Management Systems',
            desc: 'Installation and servicing of industrial and domestic waste handling systems.',
            details: 'Our team provides drainage solutions, waste treatment setups, sewer line works, and environmentally safe system integration.',
            icon: <Recycle size={32} strokeWidth={1.5} />,
            color: '#8b5cf6' // Purple
        },
        {
            title: 'Technical Supplies',
            desc: 'Reliable supply of industrial equipment, tools, and spare parts.',
            details: 'We provide high-quality technical components including mechanical parts, fittings, tools, and consumables for industrial, construction, and maintenance operations.',
            icon: <Package size={32} strokeWidth={1.5} />,
            color: '#eab308' // Gold/Yellow
        }
    ];

https://images.unsplash.com/photo-1513828583688-c52646db42da?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80
https://images.unsplash.com/photo-1599508704512-2f19efd1e35f?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80
https://images.unsplash.com/photo-1581092160562-40aa08e78837?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80
https://images.unsplash.com/photo-1504917595217-d4dc5ebe6122?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80
    const renderOrders = () => {
        const filteredOrders = orders.filter(order => {
            const term = (searchTerm || '').toString().trim().toLowerCase();
            if (!term) return true;
            return (
                (order.id && order.id.toString().includes(term)) ||
                (order.customer?.name && order.customer.name.toLowerCase().includes(term)) ||
                (order.items && order.items.some(item =>
                    item.product?.name?.toLowerCase().includes(term)
                )) ||
                (order.status && order.status.toLowerCase().includes(term))
            );
        });

        // Calculate stats from actual orders
        const pendingOrders = orders.filter(o => o.status === 'pending').length;
        const completedOrders = orders.filter(o => o.status === 'completed').length;
        const shippedOrders = orders.filter(o => o.status === 'shipped').length;
        const cancelledOrders = orders.filter(o => o.status === 'cancelled').length;

        return(
            <div className="space-y-6">
                <div className="page-header">
                    <div>
                        <h2 className="heading">Orders Management</h2>
                        <p className="muted">Track and manage all customer orders</p>
                    </div>
                    <div className="actions">
                        <button className="btn btn-default"><Filter /> Filter</button>
                        <button onClick={() => handleExport(filteredOrders, "orders-export.csv")} className="btn btn-default"><Download /> Export</button>
                    </div>
                </div>

                <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
                    <div className="card quick-stat">
                        <Clock />
                        <div>
                            <p className="muted small">Pending Orders</p>
                            <p className="large">{pendingOrders}</p>
                        </div>
                    </div>
                    <div className="card quick-stat">
                        <CheckCircle />
                        <div>
                            <p className="muted small">Completed Orders</p>
                            <p className="large">{completedOrders}</p>
                        </div>
                    </div>
                    <div className="card quick-stat">
                        <Package />
                        <div>
                            <p className="muted small">Shipped Orders</p>
                            <p className="large">{shippedOrders}</p>
                        </div>
                    </div>
                    <div className="card quick-stat">
                        <AlertCircle />
                        <div>
                            <p className="muted small">Cancelled Orders</p>
                            <p className="large">{cancelledOrders}</p>
                        </div>
                    </div>
                </div>

                <div className="card">
                    <div className="search-grid">
                        <div className="search-input">
                            <Search />
                            <input
                                type="text"
                                placeholder="Search orders..."
                                value={searchTerm}
                                onChange={(e) => setSearchTerm(e.target.value)}
                            />
                        </div>
                        <select className="select">
                            <option value="">All Statuses</option>
                            <option value="pending">Pending</option>
                            <option value="completed">Completed</option>
                            <option value="shipped">Shipped</option>
                            <option value="cancelled">Cancelled</option>
                        </select>
                        <select className="select">
                            <option value="">Date Range</option>
                            <option value="today">Today</option>
                            <option value="week">This Week</option>
                            <option value="month">This Month</option>
                        </select>
                        <button className="btn btn-default">Reset Filters</button>
                    </div>
                </div>

                {ordersLoading ? (
                    <div className="card">
                        <div className="text-center py-12">
                            <div className="spinner mx-auto mb-4"></div>
                            <p className="muted">Loading orders...</p>
                        </div>
                    </div>
                ) : (
                    <div className="card table-card">
                        <div className="table-wrap">
                            <table className="table">
                                <thead>
                                <tr>
                                    <th>Order ID</th>
                                    <th>Customer</th>
                                    <th>Items</th>
                                    <th>Amount</th>
                                    <th>Payment Method</th>
                                    <th>Priority</th>
                                    <th>Status</th>
                                    <th>Date</th>
                                    <th>Actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {filteredOrders.length === 0 ? (
                                    <tr>
                                        <td colSpan="9" className="text-center py-8 text-gray-500">
                                            No orders found
                                        </td>
                                    </tr>
                                ) : (
                                    filteredOrders.map((order) => (
                                        <tr
                                            key={order.id}
                                            style={{ cursor: "pointer" }}
                                            onClick={() => openModal('order-view', order)}
                                        >
                                            <td className="font-medium">#{order.id}</td>
                                            <td>
                                                <div>
                                                    <p className="font-medium">{order.customer?.name}</p>
                                                    <p className="text-sm text-gray-500">{order.customer?.email}</p>
                                                </div>
                                            </td>
                                            <td className="muted">
                                                <div className="max-w-xs">
                                                    {order.items?.map(item => item.product?.name).join(', ') || 'No items'}
                                                </div>
                                            </td>
                                            <td className="font-medium">
                                                Ksh {parseFloat(order.total || 0).toLocaleString()}
                                            </td>
                                            <td className="text-sm">
                                                {order.paymentMethod?.replace('_', ' ') || 'N/A'}
                                            </td>
                                            <td>
          <span className={`badge ${order.priority === 'high' ? 'badge-high' : order.priority === 'medium' ? 'badge-medium' : 'badge-low'}`}>
            {order.priority}
          </span>
                                            </td>
                                            <td>
                                                <select
                                                    value={order.status}
                                                    onClick={e => e.stopPropagation()} // Prevent row click when changing status
                                                    onChange={async (e) => {
                                                        e.stopPropagation(); // prevent row click
                                                        const newStatus = e.target.value;
                                                        try {
                                                            await updateOrder(order.id, { status: newStatus });
                                                            setOrders(prev =>
                                                                prev.map(o => (o.id === order.id ? { ...o, status: newStatus } : o))
                                                            );
                                                            showToastMessage(`Order status updated to ${newStatus}`);
                                                        } catch (error) {
                                                            console.error(error);
                                                            showToastMessage('Failed to update order status', 'error');
                                                        }
                                                    }}
                                                    className={`status-select ${getStatusColor(order.status)}`}
                                                >
                                                    <option value="pending">Pending</option>
                                                    <option value="completed">Completed</option>
                                                    <option value="shipped">Shipped</option>
                                                    <option value="cancelled">Cancelled</option>
                                                </select>
                                            </td>
                                            <td className="muted">
                                                {order.created_at ? new Date(order.created_at).toLocaleDateString() : 'N/A'}
                                            </td>
                                            <td>
                                                <div className="action-group">
                                                    <button
                                                        onClick={e => { e.stopPropagation(); openModal('order-view', order); }}
                                                        title="View"
                                                    >
                                                        <Eye />
                                                    </button>
                                                    <button
                                                        onClick={e => { e.stopPropagation(); openModal('order-edit', order); }}
                                                        title="Edit"
                                                    >
                                                        <Edit />
                                                    </button>
                                                    <button
                                                        onClick={async (e) => {
                                                            e.stopPropagation();
                                                            if (confirm('Are you sure you want to delete this order?')) {
                                                                try {
                                                                    await deleteOrder(order.id);
                                                                    await refetchOrders();
                                                                    showToastMessage('Order deleted successfully');
                                                                } catch (error) {
                                                                    showToastMessage('Failed to delete order', 'error');
                                                                }
                                                            }
                                                        }}
                                                        title="Delete"
                                                        className="danger"
                                                    >
                                                        <Trash2 />
                                                    </button>
                                                </div>
                                            </td>
                                        </tr>
                                    ))
                                )}
                                </tbody>

                            </table>
                        </div>

                        <div className="pagination">
                            <p>
                                Showing <strong>{Math.min(filteredOrders.length, 10)}</strong> of <strong>{filteredOrders.length}</strong> results
                            </p>
                            <div className="pagination-controls">
                                <button className="btn page-btn">Previous</button>
                                <button className="btn page-btn active">1</button>
                                <button className="btn page-btn">2</button>
                                <button className="btn page-btn">3</button>
                                <button className="btn page-btn">Next</button>
                            </div>
                        </div>
                    </div>
                )}
            </div>
        );
    };